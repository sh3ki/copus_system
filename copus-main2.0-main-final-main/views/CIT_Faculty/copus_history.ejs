<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CIT Faculty History</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color-start: #0f2027; /* Dark blue */
            --primary-color-end: #203a43; /* Darker blue */
            --secondary-color-start: #2c5364; /* Blue-green */
            --secondary-color-end: #203a43; /* Darker blue */
            --text-light: #e0e0e0;
            --text-dark: #333;
            --sidebar-width: 280px;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, var(--primary-color-start), var(--primary-color-end));
            color: var(--text-light);
            min-height: 100vh;
        }

        /* Header styles */
        header {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            position: fixed;
            top: 0;
            left: var(--sidebar-width);
            right: 0;
            z-index: 1000;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
        }
        
        .logo {
            display: flex;
            align-items: center;
            font-weight: 600;
            font-size: 1.5rem;
            color: #fff;
        }
        .logo img {
            height: 40px;
            margin-right: 15px;
        }

        .header-title {
            font-size: 1.25rem;
            color: var(--text-light);
            margin-left: -150px;
        }
        
        .header-icons {
            display: flex;
            align-items: center;
            gap: 1.5rem;
        }
        .header-icons i {
            font-size: 1.5rem;
            color: var(--text-light);
            cursor: pointer;
            transition: color 0.3s;
        }
        .header-icons i:hover {
            color: #17e891;
        }
        .user-icon-circle {
            background: rgba(255, 255, 255, 0.2);
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            font-size: 1.25rem;
        }

        /* Sidebar styles */
        .custom-sidebar {
            width: var(--sidebar-width);
            position: fixed;
            top: 0;
            left: 0;
            bottom: 0;
            background: linear-gradient(to bottom, #11405b, #0f2027);

            color: var(--text-light);
            padding: 2rem 1.5rem;
            display: flex;
            flex-direction: column;
            box-shadow: 2px 0 15px rgba(0, 0, 0, 0.3);
            z-index: 1001;
        }

        .custom-sidebar h6 {
            font-size: 1.2rem;
            font-weight: 600;
            color: #fff;
            margin-bottom: 0.25rem;
        }
        .custom-sidebar small {
            color: rgba(255, 255, 255, 0.6);
        }
        .custom-sidebar hr {
            border-color: rgba(255, 255, 255, 0.1);
            margin: 1.5rem 0;
        }
        .custom-sidebar a {
            color: var(--text-light);
            text-decoration: none;
            padding: 0.75rem 1rem;
            margin-bottom: 0.5rem;
            border-radius: 8px;
            transition: background 0.3s, color 0.3s;
            display: flex;
            align-items: center;
            gap: 1rem;
        }
        .custom-sidebar a i {
            font-size: 1.25rem;
        }
        .custom-sidebar a:hover {
            background: rgba(255, 255, 255, 0.1);
            color: #17e891;
        }
        .custom-sidebar a.active {
            background: linear-gradient(to right, #2c5364, #203a43);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            color: #fff;
        }
        .logout-btn {
            background: linear-gradient(to right, #e74c3c, #c0392b);
            border: none;
            color: #fff;
            padding: 0.75rem 1rem;
            border-radius: 8px;
            text-align: left;
            width: 100%;
            font-weight: 600;
            transition: transform 0.2s;
        }
        .logout-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        /* Main content styles */
        .main-content {
            margin-left: var(--sidebar-width);
            padding: 7rem 2rem 2rem 2rem;
            margin-top: -60px;
        }
        
        .main-content h1 {
            font-size: 2.5rem;
            font-weight: 600;
            color: #fff;
            margin-bottom: 2rem;
            text-align: center;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        /* Table styles */
        .table-container {
            width: 100%;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
            padding: 20px;
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
            margin-top: 20px;
        }

        thead tr {
            background-color: rgba(255, 255, 255, 0.1);
        }

        th, td {
            padding: 15px;
            text-align: left;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            white-space: nowrap; /* Prevent text from wrapping */
        }
        
        th {
            font-weight: 600;
            color: #17e891;
            text-transform: uppercase;
        }

        td {
            color: var(--text-light);
        }

        tbody tr:hover {
            background-color: rgba(255, 255, 255, 0.05);
        }
        
        .table-header {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 1rem;
        }

        #searchInput {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid rgba(255, 255, 255, 0.2);
            color: var(--text-light);
            border-radius: 8px;
            padding: 0.5rem 1rem;
        }

        #searchInput::placeholder {
            color: rgba(255, 255, 255, 0.5);
        }
        
        /* Status Badges */
        .status-completed {
            color: #2ecc71; /* Green */
            font-weight: 600;
        }
        .status-cancelled {
            color: #e74c3c; /* Red */
            font-weight: 600;
        }
        .status-pending {
            color: #f1c40f; /* Yellow */
            font-weight: 600;
        }
        .status-submitted {
            color: #3498db; /* Blue */
            font-weight: 600;
        }
        .status-draft {
            color: #9b59b6; /* Purple */
            font-weight: 600;
        }
        
        /* Badge styles */
        .badge {
            padding: 0.25rem 0.5rem;
            border-radius: 0.375rem;
            font-size: 0.75rem;
            font-weight: 500;
        }

        /* No results message in table */
        tbody td[colspan="12"] {
            font-style: italic;
            color: rgba(255, 255, 255, 0.6);
            padding: 2rem;
        }
        /* --- Form Controls/Filters --- */
/* Filter Container */
.filter-container {
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(5px);
    border-radius: 15px;
    padding: 2rem;
    margin-bottom: 2rem;
    border: 1px solid rgba(255, 255, 255, 0.1);
}

/* Form controls (inputs and selects) */
.form-control {
    background-color: rgba(255, 255, 255, 0.1);
    border: 1px solid rgba(255, 255, 255, 0.2);
    color: #fff;
    border-radius: 8px;
    padding: 0.75rem 1rem;
    font-size: 0.9rem;
    height: 42px; /* Uniform height for all inputs */
    box-shadow: none;
    transition: all 0.3s ease;
}

.form-control::placeholder {
    color: rgba(255, 255, 255, 0.5);
}

.form-control:focus {
    background-color: rgba(255, 255, 255, 0.15);
    border-color: var(--accent-color);
    box-shadow: 0 0 0 0.25rem rgba(23, 232, 145, 0.25);
    color: #fff;
}

/* Dropdown options styling */
.form-control option {
    background: var(--primary-color-start);
    color: #fff;
}

/* Filter Group Overrides */
.filter-group .form-control {
    width: 100%; /* Fill the grid column */
}

/* Grid Layout for Filters */
.filter-row {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 1.5rem;
    align-items: end;
}

/* Clear Button Styling */
.filter-group:last-child {
    display: flex;
    flex-direction: column;
    justify-content: flex-end; /* Align button to bottom */
}

.btn-clear {
    background-color: #ff4d4f; /* Red button */
    color: white;
    border: none;
    padding: 0.75rem 1rem;
    border-radius: 8px;
    cursor: pointer;
    font-size: 0.9rem;
    width: 100%; /* Fill column */
    height: 42px; /* Match input height */
    transition: all 0.3s ease;
}

.btn-clear:hover {
    background-color: #e53935;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
    .filter-row {
        grid-template-columns: 1fr;
    }
}

        /* Action buttons */
        .action-btn {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 0.85rem;
            transition: all 0.3s ease;
            margin: 0.25rem 0;
            display: inline-block;
            white-space: nowrap;
        }

        .btn-make-appointment {
            background-color: #3498db;
            color: #fff;
        }

        .btn-make-appointment:hover {
            background-color: #2980b9;
            transform: translateY(-2px);
        }

        /* Modal styles */
        .modal {
            display: none;
            position: fixed;
            z-index: 2000;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(5px);
        }

        .modal-content {
            background: linear-gradient(135deg, var(--primary-color-start), var(--primary-color-end));
            margin: 5% auto;
            padding: 2rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 15px;
            width: 80%;
            max-width: 600px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.5);
        }

        .close {
            color: var(--text-light);
            float: right;
            font-size: 2rem;
            font-weight: bold;
            cursor: pointer;
        }

        .close:hover,
        .close:focus {
            color: #e74c3c;
        }

        .modal h2 {
            color: #fff;
            margin-bottom: 1.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--text-light);
            font-weight: 500;
        }

        .btn-submit {
            background: linear-gradient(to right, #2ecc71, #27ae60);
            color: #fff;
            padding: 0.75rem 2rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: 600;
            width: 100%;
            transition: all 0.3s ease;
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

    </style>
</head>
<body>
   

    <div class="custom-sidebar">
        <h6>Welcome! <%= firstName %>, <%= lastName %></h6>
        <small><%= employeeId %></small>
        <hr>
        <a href="/CIT_Faculty_dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a>
        <a href="/CIT_Faculty_copus_history" class="active"><i class="bi bi-clock-history"></i>Copus History & Results</a>
        <a href="/CIT_Faculty_copus_appointments"><i class="bi bi-calendar-check"></i> COPUS Appointments</a>
        <a href="/CIT_Faculty_schedule_management"><i class="bi bi-calendar-event-fill"></i>My Schedule</a>
        <a href="/CIT_Faculty_setting"><i class="bi bi-gear-fill"></i> Account & Profile Settings</a>
        <hr>
        <form action="/logout" method="POST">
            <button type="submit" class="logout-btn">Logout</button>
        </form>
    </div>

    <main class="main-content">
    <h1>PERSONAL COPUS HISTORY</h1>


            <!-- Filter Controls -->
            <div class="filter-container">
                <div class="filter-row">
                    <div class="filter-group">
                        <label for="searchInput">Search Faculty/Subject</label>
                        <input type="text" id="searchInput" placeholder="Search by faculty name, subject, observer..." class="form-control">
                    </div>
                    <div class="filter-group">
                        <label for="copusFilter">COPUS Type</label>
                        <select id="copusFilter" class="form-control">
                            <option value="">All COPUS Types</option>
                            <option value="COPUS 1">COPUS 1</option>
                            <option value="COPUS 2">COPUS 2</option>
                            <option value="COPUS 3">COPUS 3</option>
                        </select>
                    </div>
                    <div class="filter-group">
                        <label for="yearFilter">Observation Year</label>
                        <select id="yearFilter" class="form-control">
                            <option value="">All Years</option>
                            <option value="2024">2024</option>
                            <option value="2023">2023</option>
                            <option value="2022">2022</option>
                            <option value="2021">2021</option>
                        </select>
                    </div>
                    <div class="filter-group">
                        <label for="sortBy">Sort By</label>
                        <select id="sortBy" class="form-control">
                            <option value="date-desc">Latest First</option>
                            <option value="date-asc">Oldest First</option>
                            <option value="grade-desc">Highest Grade First</option>
                            <option value="grade-asc">Lowest Grade First</option>
                            <option value="faculty-asc">Faculty Name A-Z</option>
                            <option value="faculty-desc">Faculty Name Z-A</option>
                            <option value="subject-asc">Subject A-Z</option>
                        </select>
                    </div>
                    <div class="filter-group">
                        <label>&nbsp;</label>
                        <button type="button" class="btn-clear" onclick="clearFilters()">Clear All</button>
                    </div>
                </div>
            </div>

    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>FACULTY NAME</th>
                    <th>DEPARTMENT</th>
                    <th>OBSERVATION DATE</th>
                    <th>SUBJECT</th>
                    <th>YEAR</th>
                    <th>SEMESTER</th>
                    <th>ROOM</th>
                    <th>OBSERVER</th>
                    <th>COPUS TYPE</th>
                    <th>STUDENT ACTION %</th>
                    <th>TEACHER ACTION %</th>
                    <th>ENGAGEMENT %</th>
                    <th>OVERALL %</th>
                    <th>FINAL RATING</th>
                    <th>STATUS</th>
                    <th>SUBMITTED AT</th>
                    <th>ACTIONS</th>
                </tr>
            </thead>
            <tbody id="historyTableBody">
                <% if (copusResults && copusResults.length > 0) { %>
                    <% copusResults.forEach(result => { %>
                        <tr>
                            <td><%= result.faculty_name || 'N/A' %></td>
                            <td><%= result.faculty_department || 'N/A' %></td>
                            <td><%= new Date(result.observation_date).toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' }) %></td>
                            <td><%= result.subject_name || 'N/A' %></td>
                            <td><%= result.year || 'N/A' %></td>
                            <td><%= result.semester || 'N/A' %></td>
                            <td><%= result.room || 'N/A' %></td>
                            <td><%= result.observer_name || 'N/A' %></td>
                            <td><%= result.copus_type || 'N/A' %></td>
                            <td><strong><%= result.student_action_percentage !== undefined ? result.student_action_percentage.toFixed(1) : '0.0' %>%</strong></td>
                            <td><strong><%= result.teacher_action_percentage !== undefined ? result.teacher_action_percentage.toFixed(1) : '0.0' %>%</strong></td>
                            <td><strong><%= result.engagement_level_percentage !== undefined ? result.engagement_level_percentage.toFixed(1) : '0.0' %>%</strong></td>
                            <td><strong><%= result.calculated_overall_percentage !== undefined ? result.calculated_overall_percentage.toFixed(1) : (result.overall_percentage ? result.overall_percentage.toFixed(1) : '0.0') %>%</strong></td>
                            <td>
                                <% 
                                    let ratingClass = 'badge badge-info';
                                    let rating = result.final_rating || 'N/A';
                                    if (rating === 'Great') ratingClass = 'badge badge-success';
                                    else if (rating === 'Good') ratingClass = 'badge badge-primary';
                                    else if (rating === 'Needs Improvement') ratingClass = 'badge badge-warning';
                                    else if (rating === 'Unsatisfactory') ratingClass = 'badge badge-danger';
                                %>
                                <span class="<%= ratingClass %>"><%= rating %></span>
                            </td>
                            <td class="status-<%= result.status || 'unknown' %>"><%= (result.status || 'N/A').toUpperCase() %></td>
                            <td><%= result.submitted_at ? new Date(result.submitted_at).toLocaleDateString('en-US', { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' }) : 'N/A' %></td>
                            <td>
                                <button class="action-btn btn-make-appointment" onclick="openMakeAppointmentModal('<%= result._id %>', '<%= result.observer_id %>', '<%= result.observer_name %>')">
                                    <i class="bi bi-calendar-plus"></i> Make Appointment
                                </button>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="17" style="text-align: center;">No COPUS results found.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</main>

    <!-- Make Appointment Modal -->
    <div id="makeAppointmentModal" class="modal">
        <div class="modal-content">
            <span class="close" onclick="closeMakeAppointmentModal()">&times;</span>
            <h2>Make Appointment</h2>
            <form id="makeAppointmentForm">
                <input type="hidden" id="copusResultId" name="copus_result_id">
                <input type="hidden" id="observerId" name="observer_id">
                <input type="hidden" id="observerName" name="observer_name">
                <div class="form-group">
                    <label for="appointmentDate">Appointment Date</label>
                    <input type="date" id="appointmentDate" name="appointment_date" class="form-control" required>
                </div>
                <div class="form-group">
                    <label for="appointmentTime">Appointment Time</label>
                    <input type="time" id="appointmentTime" name="appointment_time" class="form-control" required>
                </div>
                <button type="submit" class="btn-submit">Submit Appointment</button>
            </form>
        </div>
    </div>

    <script>
        function openMakeAppointmentModal(copusResultId, observerId, observerName) {
            document.getElementById('copusResultId').value = copusResultId;
            document.getElementById('observerId').value = observerId;
            document.getElementById('observerName').value = observerName;
            
            // Set min date to today
            const today = new Date().toISOString().split('T')[0];
            document.getElementById('appointmentDate').setAttribute('min', today);
            
            document.getElementById('makeAppointmentModal').style.display = 'block';
        }

        function closeMakeAppointmentModal() {
            document.getElementById('makeAppointmentModal').style.display = 'none';
            document.getElementById('makeAppointmentForm').reset();
        }

        // Close modal when clicking outside
        window.onclick = function(event) {
            const modal = document.getElementById('makeAppointmentModal');
            if (event.target === modal) {
                closeMakeAppointmentModal();
            }
        }

        // Handle appointment form submission
        document.getElementById('makeAppointmentForm').addEventListener('submit', async function(e) {
            e.preventDefault();
            
            const formData = {
                copus_result_id: document.getElementById('copusResultId').value,
                observer_id: document.getElementById('observerId').value,
                observer_name: document.getElementById('observerName').value,
                appointment_date: document.getElementById('appointmentDate').value,
                appointment_time: document.getElementById('appointmentTime').value
            };

            try {
                const response = await fetch('/faculty_create_appointment', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify(formData)
                });

                if (response.ok) {
                    alert('Appointment created successfully!');
                    closeMakeAppointmentModal();
                    // Optional: redirect to appointments page
                    window.location.href = '/CIT_Faculty_copus_appointments';
                } else {
                    const data = await response.json();
                    alert('Error: ' + (data.message || 'Failed to create appointment'));
                }
            } catch (error) {
                console.error('Error:', error);
                alert('Error creating appointment');
            }
        });

        document.addEventListener('DOMContentLoaded', function() {
            const searchInput = document.getElementById('searchInput');
            const tableBody = document.getElementById('historyTableBody');
            const rows = tableBody.getElementsByTagName('tr');

            searchInput.addEventListener('keyup', function() {
                const filter = searchInput.value.toLowerCase();
                let hasVisibleRow = false;

                for (let i = 0; i < rows.length; i++) {
                    const row = rows[i];
                    if (row.cells.length < 2) continue; // Skip the "No data" row if it exists

                    let rowText = '';
                    for (let j = 0; j < row.cells.length; j++) {
                        rowText += row.cells[j].textContent.toLowerCase() + ' ';
                    }

                    if (rowText.includes(filter)) {
                        row.style.display = '';
                        hasVisibleRow = true;
                    } else {
                        row.style.display = 'none';
                    }
                }
            });
        });
    </script>
</body>
</html>